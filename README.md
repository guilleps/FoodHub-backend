# FoodHub backend

Exec `docker-compose up` to start the application database service.

How to access the endpoints documentation?
Enter to the following URL in your browser: `http://localhost:8000/swagger-ui.html` to access the Swagger UI documentation for the API endpoints.